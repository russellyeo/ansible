# Radarr container
# Docker Hub: https://hub.docker.com/r/linuxserver/radarr
# Docs: https://docs.linuxserver.io/images/docker-radarr
---
- name: Start container
  tags:
    - radarr
    - container
  docker_container:
    name: radarr
    image: linuxserver/radarr
    state: "{{ docker_state }}"
    restart: "{{ docker_restart }}"
    restart_policy: "{{ docker_restart_policy }}"
    recreate: "{{ docker_recreate }}"
    volumes:
      - "{{ config_directory }}/radarr:/config"
      - "{{ data_directory }}:/data"
    networks:
      - name: "{{ docker_network }}"
    dns_servers:
      - "{{ dns_primary }}"
      - "{{ dns_secondary }}"
    env:
      PUID: "{{ servarr_uid }}"
      PGID: "{{ servarr_gid }}"
      TZ: "{{ timezone }}"
    labels:
      traefik.enable: "false"
      traefik.http.routers.radarr.rule: "Host(`radarr.{{ tailnet_synology_machine }}.{{ tailnet_host }}`)"
      traefik.http.routers.radarr.entrypoints: "websecure"
      traefik.http.routers.radarr.tls: "true"
      traefik.http.services.radarr.loadbalancer.server.port: "{{ host_port }}"
      homepage.group: "Media"
      homepage.name: "Radarr"
      homepage.icon: "radarr.png"
      homepage.href: "https://radarr.{{ tailnet_synology_machine }}.{{ tailnet_host }}"
      homepage.description: "Manage your movies"
      homepage.widget.type: radarr
      homepage.widget.url: "http://{{ network_localhost }}:{{ host_port }}"
      homepage.widget.key: "{{ radarr_api_key }}"
      homepage.widget.enableQueue: "false"
      
